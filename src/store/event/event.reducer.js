const initialEventState = {
  events: [],
};

const eventReducer = (state = initialEventState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default eventReducer;
